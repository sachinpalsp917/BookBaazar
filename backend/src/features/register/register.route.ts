import { Router } from "express";
import { AUTH_ROUTES } from "../../constants/routes";

const registerRoutes = Router();

// registerRoutes.post(AUTH_ROUTES.REGISTER);
export default registerRoutes;
